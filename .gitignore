# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# pytest / coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Node / frontend build
node_modules/
frontend/dist/
frontend/.vite/
.npm/

# Secrets / keys
.env
.env.*
*.pem
*.key

# Pipeline cache (generated — not reproducibility inputs)
data/artifacts/batch/
data/cache/
*.npz

# Model weights (too large for git — use DVC or LFS)
*.pt
*.bin
*.safetensors
models/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# mypy
.mypy_cache/

# Generated pipeline artifacts — must only be committed after a verified clean
# run (correct quiet stars, TLS detector, no KOIs in substrate).
# Use `git add -f` after manual verification.
data/artifacts/adversarial_selftest.json
data/artifacts/adversarial_selftest.manifest.json
data/artifacts/injection_recovery.json
data/artifacts/injection_recovery.manifest.json
data/artifacts/injection_recovery_completeness.png

# job.json — raw pipeline input parameters; never bundle into repository or
# client-side assets.  Use job.example.json for local development scaffolding.
job.json
**/job.json
